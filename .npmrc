# Optimize npm installations
# Use this for production deployments with: npm ci --production

# Don't install optional dependencies
optional=false

# Prefer offline cached packages when available
prefer-offline=true

# Reduce logging noise
loglevel=warn

# Use exact versions from package-lock.json
package-lock=true

# Don't save exact versions when installing
save-exact=false
